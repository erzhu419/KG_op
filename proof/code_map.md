# Code-To-Theory Map

## Mathematical Objects

| Theory object | Current code object | Status |
| --- | --- | --- |
| Decision vector `x` | `problem` candidates, integer tuples | Implemented |
| Policy-state summary `s(x)` | `policy_state(x)`, `SyntheticPolicyStateEncoder`, `SelfSupervisedPolicyStateEncoder` | Synthetic and learned encoder paths implemented |
| Provider coordinate `psi(x)=(A(x),N(x))` | `core.cumulative_risk.CumulativeRiskFeatureProvider` / `RiskExposure` | Implemented as the main interface |
| Observable state/trajectory record `e(x)` | `representation.observable_exposure.ObservableStateExposure` and each domain's `observable_state_exposure` adapter | Reads only declared control/state groups, occupancy, transitions, and low-frequency trajectory summaries; no target outcome or target risk provider |
| Equivariant canonical channel roles | `representation.channel_role_alignment.EquivariantChannelRoleAligner` and `role_aligned_observable_state_descriptor` | Source exposure distributions learn role prototypes; source margins only orient role numbering; held-out matching uses a deterministic unlabeled target exposure pool. `aligned_role_values_equivariant` and `equivariant_role_matching_preserves_descriptor` prove invariance under simultaneous channel/assignment permutation. |
| Observable constraint-mean coordinate `phi_mu(x)` | `representation.boundary_coordinate.SourceAlignedBoundaryCoordinate` / `ObservableConstraintMeanBasis` | Frozen from ordinary replicated source simulations; target coefficients use only charged observations and remain separate from cumulative HVD |
| Observable cumulative-variance coordinate `psi_v(x)` | `representation.variance_coordinate.SourceAlignedVarianceRiskCoordinate` | Independently aligned from source replicated log-variance strata using the same observable `e(x)`; emits nonnegative `A` and soft shared regimes `N` for factor-HVD without target truth/provider access |
| Source boundary excitation | `LearnedMetaPrior.source_design_mode=universal_mixture`, frozen generic low-frequency policy library, and source sign-support diagnostics | Oracle-free structural design; source calls are charged and random-source control is retained |
| Idiosyncratic exposure `A(x)` | `risk_exposures(x).A` | Implemented for factor synthetic, inventory, queue, and traffic proxy/CSV |
| Shared-shock exposure `N(x)` | `risk_exposures(x).N` | Implemented for factor synthetic, inventory, queue, and traffic proxy/CSV |
| `Lambda`, `B`, `omega` | `cumulative_risk_parameters()` | Implemented for synthetic |
| `v_C(x)` | `true_cumulative_risk_decomposition()["total"]` | Implemented for synthetic |
| HVD predictor `\hat v_C(x)` | `OrthogonalHVD(mode="factor")` cumulative beta | Implemented for synthetic |
| Certified bound | `core.certification.conservative_chance_margin` with `predict_certification_variance()` | Implemented for theory/legacy modes |
| SC candidate generation | `state_anchor_points()` and `inverse_state_anchor()` | Synthetic implemented |
| Self-supervised trajectory representation | `SelfSupervisedTrajectoryEncoder`, `TransformerTrajectoryEncoder` | Implemented for masked, contrastive, and attention-style pooling ablations |
| Boundary-aligned LODO representation | `BoundaryAlignedRiskSubspaces`, compact source-expert mixture, frozen source-boundary episode admission, target nested-LOO diagnostics | Implemented; paired N=40 KG promotion matrix is running |
| Traffic occupancy encoder | `TrafficTrajectoryEncoder` plus `sumo_sim.py` trajectory logger | Implemented for fresh-seed CSV schema; large trajectory table requires server-generated logs |
| Adaptive Stage-I exact KG | `SingleOLHKGAlgorithm._exact_posterior_update_scores` | Adaptive search acquisition after `n0` via `acquisition_mode=exact_mc`; it is not a claim about the initial design or reserved verification suffix |
| Two-stage terminal decision | `finalist_replication_budget`, fixed finalist universe, `_replicated_finalist_recommendation_index`, `two_stage_decision` diagnostics | Main deployed decision architecture; certified and uncertified outputs have distinct theorem claims |
| Finite task-structure posterior `Q_t(xi)` | `representation.task_posterior.FiniteTaskPosterior` | Implemented behind `task_posterior_mode=finite`; FactorShock N=20 Gate 1 promoted on 2026-07-11, cross-domain Gate 2 pending |
| Expert-specific surrogate state | `FiniteTaskModelEnsemble` / `TaskExpertState` | Frozen source expert basis plus independent GPR/HVD per expert |
| Task-robust cumulative certificate | `FiniteTaskModelEnsemble.robust_moments_many` | Within/between variance plus forward-KL robust upper moments |

## Implementation Notes

`factor` HVD is now the only mode that consumes cumulative-risk linear
features.  `pooled`, `class`, and `orthogonal` remain pointwise residual
variance estimators, which makes the ablation clean:

- `pooled`: no heteroscedastic structure.
- `class`: regime-level heteroscedasticity.
- `orthogonal`: smooth low-dimensional log-variance.
- `factor`: cumulative trajectory/meta variance with shared shocks.

The current high-dependence path uses two noninterchangeable coordinates.
Factor synthetic, inventory, queue, and traffic expose `psi(x)=(A,N)` through
`CumulativeRiskFeatureProvider`; factor-HVD, state candidate anchors,
certification variance, and exact-KG variance updates consume `psi`.  A frozen
source-learned observable coordinate `eta(x)` drives only the constraint-mean
GPR and its epistemic uncertainty.  They meet in one chance margin, rather than
assuming that `psi` must also be sufficient for the conditional mean.  The
factor synthetic is the oracle-clean case where the true constraint
variance is exactly

```text
v_C(x) = A(x)^T Lambda A(x) + N(x)^T B N(x) + N(x)^T omega + floor.
```

This gives the proof and experiments one place where the shared-shock term
`N^T B N` is not merely a metaphor.

The default paper-style feasibility certificate is now the theory bound

```text
mu_g(x) + sqrt(beta_g) s_g(x) + z_alpha sqrt(v_C^+(x)) <= tau,
```

where `s_g` is the constraint GPR posterior standard deviation and `v_C^+`
comes from HVD certification variance.  Legacy mode remains available only as
an ablation.

## Code-Level Proof Bridges

| Python implementation | Formula implemented | Lean bridge |
| --- | --- | --- |
| `core.gpr.ParametricGPR.update` | rank-one Kalman mean update `m'(u)=m(u)+cov(u,x)/(sigma2+cov(x,x))*(y-m(x))` | `SCOLHKG.Real.GPRUpdate.rank_one_update_standard_shock_slope` |
| `core.kg.compute_kg_vectorized` | KG slope `sigma_tilde(u;x)=cov(u,x)/sqrt(sigma2+cov(x,x))` under a standard predictive shock | `SCOLHKG.Real.GPRUpdate.kg_sigma_tilde_matches_code_formula` plus `rank_one_update_standard_shock_slope` |
| `SingleOLHKGAlgorithm._replication_candidates` | an observed point with epistemic variance `q` and observation variance `r` offers posterior variance reduction `q^2/(q+r)`; exact KG, rather than a heuristic bonus, decides whether to repeat it | `SCOLHKG.Real.GPRUpdate.replication_variance_update_identity`, `replication_variance_reduction_nonnegative`, and `replication_variance_reduction_le_epistemic` |
| `core.gpr.ParametricGPR._project_covariance_psd` and stabilized `update` denominator | a negative numerical quadratic form is clipped to zero before adding strictly positive observation variance | `SCOLHKG.Real.GPRUpdate.stabilizedQuadraticVariance_nonnegative`, `stabilizedQuadraticVariance_eq_self`, and `stabilizedPredictiveVariance_positive` |
| `core.kg.compute_h` | `E[max_j a_j+b_j Z]-max_j a_j` line-envelope KG value | `SCOLHKG.Real.LineEnvelopeKG.certified_lineEnvelopeKG_exact` proves exactness once the active hull certificate is available |
| `core.kg.compute_h_certificate`, `validate_h_certificate` | active stack hull, cuts, Gaussian masses, endpoint/tail-slope dominance checks | `SCOLHKG.Real.LineEnvelopeStack` proves the validator conditions imply active-line certificates |
| `core.kg.compute_h_certificate(...).trace` | per-step stack snapshots for candidate, break, pop, and push actions | `SCOLHKG.Real.LineEnvelopeAlgorithm` proves pop/push preserve slope and cut order under the Python branch conditions |
| final `compute_h` hull state | active atoms whose lines dominate all original/processed lines at interval endpoints | `SCOLHKG.Real.LineEnvelopeGlobal` proves final global dominance implies atom certificates and exact line-envelope KG without a runtime validator |
| `core.kg._build_line_envelope` intersection `z=(a_old-a_new)/(b_new-b_old)` | old active line dominates left of the cut; new line dominates right of the cut; pop and right-tail split preserve certificates over every point of the affected interval/tail | `SCOLHKG.Real.LineEnvelopeIntersection` proves the concrete intersection arithmetic, popped-cell takeover, right-tail finite/tail cell construction, and interval/tail dominance |
| `representation.risk_aligned_subspace.BoundaryAlignedRiskSubspaces` | transfer projectors instead of named latent axes; orthogonal source/target rotations leave `UU^T` unchanged | `SCOLHKG.Real.RiskAlignedRepresentation.subspaceProjector_rotation_invariant` |
| `TransferableSpectralBasis` retained-eigenvalue whitening | discard numerically null directions and whiten only the identifiable rank | `SCOLHKG.Real.RiskAlignedRepresentation.retainedWhitening_orthonormal` |
| compact source risk-expert ensemble | nonnegative weights summing to one mix normalized source chance-boundary scores | `SCOLHKG.Real.RiskAlignedRepresentation.simplexExpertMixture_mem_interval` |
| `PilotGatedMetaPriorBasis._nested_alignment_loo_predictions` | refit the complete target alignment after removing each held-out pilot label | `SCOLHKG.Real.RiskAlignedRepresentation.nestedLOO_refit_does_not_read_heldout_label` |
| guarded additive group gate | strong heredity plus source-support floor; rejected groups return the exact prior model | `SCOLHKG.Real.RiskAlignedRepresentation.strongHeredity_survives_interaction_filter` and `weakSourceSupport_is_exact_fallback` |
| target alignment/frequency evidence gate | require observed feasible and infeasible chance margins; one-sided pilots and rejected adapters leave the Stage-1 basis unchanged, then may be reconsidered after a fixed observation interval | `SCOLHKG.Real.RiskAlignedRepresentation.noFeasibleEvidence_is_exact_fallback` and `noInfeasibleEvidence_is_exact_fallback` |
| `SourceBoundaryEpisodePrior` plus frozen profile replay | source-only, disjoint pilot/evaluation episodes may replace unstable target gain evidence, but cannot replace two-sided target support or target safety checks; proposals do not read target labels | `SCOLHKG.Real.RiskAlignedRepresentation.sourceEpisodeAdmission_requires_twoSided`, `sourceEpisodeAdmission_requires_targetSafety`, `sourceEpisodeSupport_replaces_gainEvidence_only`, and `sourceOnlyProposal_targetLabel_invariant` |
| `benchmark_lodo_meta_prior.meta_source_seed`, paper mode | source histories and the learned prior are fixed across held-out target evaluation seeds; target-seed-dependent source retraining is sensitivity-only | `SCOLHKG.Real.RiskAlignedRepresentation.sourceOnlyProposal_targetLabel_invariant` |
| replicated `universal_mixture` source design | one target-formula-free low-frequency policy library is simulated in each source domain; one-sided source margins fail strict excitation, and source-only estimators cannot distinguish margin functions agreeing on the full source design | `SCOLHKG.Real.BoundaryExcitation.nonnegative_source_not_strictly_excited`, `source_indistinguishability_lower_bound`, and `source_only_decision_cannot_separate_agreeing_models` |
| `SingleOLHKGAlgorithm._refresh_sequential_basis` | freeze the Stage-1 fallback; an admitted feature switch rebuilds the GPR from the initial empirical prior and replays every recorded rank-one update, while rejection commits the old posterior exactly | `SCOLHKG.Real.RiskAlignedRepresentation.replayPosterior_cons`, `rejectedRepresentationSwitch_is_exact_fallback`, and `admittedRepresentationSwitch_commits_replay` |
| sorted/collapsed `_build_line_envelope` while-loop fold | recursive insert loop over active lines; popped lines remain pointwise dominated by the final output stack; final output endpoint checks lift to original input lines | `SCOLHKG.Real.LineEnvelopeFold.foldLoop_dominates_input`, `foldLoop_output_endpoint_dominance_to_finalInvariant`, and `foldLoop_lineEnvelopeKG_exact_from_output_endpoint_dominance` |
| `variance.OrthogonalHVD.update` | residual record `resid2=(y-mu)^2` | `SCOLHKG.Real.HVDImplementation.residualSquare_nonnegative` |
| `variance.OrthogonalHVD.update(..., replicate_variance=..., replicate_count=r)` | repeated evaluations replace singleton innovation residuals with nonnegative within-policy sample variance; the tail guard records `r-1` effective chi-square degrees of freedom | `SCOLHKG.Real.HVDImplementation.replicateSampleVariance_nonnegative`, `replicateVarianceDof_mono`, and `effectiveVarianceDof_append_ge` |
| `hvd_cumulative_target_evidence_mode="prequential_upper"` | freeze the target prediction before observing each online new point; use its raw squared innovation as target variance-shape evidence, while excluding initial in-sample residuals and retaining within-policy sample variance for replications | `SCOLHKG.Real.SourceShapeMixtureHVD.weighted_prequential_residual_second_moment_identity` proves that its expectation is true noise second moment plus squared prediction bias, and `true_variance_le_prequential_residual_second_moment` proves conservative moment domination |
| `decision_backend="sobol_joint_voi"` unified evaluate-or-replicate action | map the GPR covariance update through `sqrt(beta_g) * (s_old - s_new)` and the source-shape covariance update through the resulting decrease of `z_alpha * sqrt(v_C_plus)` before adding them; both terms are therefore reductions of the same chance margin in response units | `SCOLHKG.Real.SourceShapeMixtureHVD.squareRootRadiusReduction_nonnegative` proves nonnegativity under covariance contraction; `SCOLHKG.Real.GPRUpdate.replication_variance_reduction_nonnegative` supplies the rank-one GPR contraction premise |
| `meta_component_stage="spectral_hvd"` and factor-HVD target update | source-only boundary-aligned dimensionless variance shape; unlabeled target-measure normalization; prior-centered ridge with nonnegative hierarchical scale `(n*c_target + rho*c_source)/(n+rho)`; target variance-shape updates use within-policy replications or the explicitly enabled prequential conservative moment | `SCOLHKG.Real.RidgeHVD.priorCenteredPenalty_nonnegative`, `hierarchicalVarianceScale_nonnegative`, `SCOLHKG.Real.HVDImplementation.replicateSampleVariance_nonnegative`, and `SCOLHKG.Real.SourceShapeMixtureHVD.true_variance_le_prequential_residual_second_moment` |
| `observable_mean_mode="source_affine"` | preserve one complete frozen source signed-distance atom per expert; source-only episodes learn its offset/scale prior, while charged target pilots update only that affine calibration and evidence-mixture mass | `SCOLHKG.Real.SourceAffineBoundaryTransfer` proves the exact transfer-error identity, a uniform offset/scale error bound, safe-side preservation under positive scale, and sound certification after charging the transfer-error radius |
| `hvd_cumulative_transfer_mode="source_mixture"` | source-domain PSD cumulative-risk shapes form a nonnegative low-rank dictionary; admitted target variance evidence updates only its nonnegative weights and posterior covariance; certification adds the pointwise posterior shape radius | `SCOLHKG.Real.SourceShapeMixtureHVD.sourceShapeMixture_nonnegative`, `target_information_shrinks_scalar_shape_variance`, and `target_information_shrinks_scalar_shape_guard` |
| `FiniteTaskPosterior.robust_chance_margin`, `task_posterior_robust_certificate_mode="joint_tangent"` | mean, mixture epistemic variance, and aleatoric variance share one KL-admissible task law; positive square-root tangents reduce the nonlinear margin to one robust expert payoff, and the finite-grid minimum remains a valid upper bound | `SCOLHKG.Real.JointKLChanceCertificate.sqrt_le_tangent`, `taskWeightedVariance_le_centeredSecondMoment`, `shared_task_law_chance_le_robust_tangent`, and `finite_minimum_of_joint_bounds_is_valid` |
| `variance.OrthogonalHVD._fit_output`, factor mode | replication-aware IRLS weights sample variances by `dof/v(x)^2`; each accepted projected-gradient step weakly decreases the current weighted objective, PSD-projects `B`, nonnegative-projects `Lambda/floor/omega`, and predicts `floor + A^T Lambda A + N^T B N + N^T omega` | `SCOLHKG.Real.HVDImplementation.weightedSquaredResidualRisk_nonnegative`, `acceptedProjectedIterations_objective_le`, `acceptedProjectedIterations_feasible`, `SCOLHKG.Real.RidgeHVD.ridge_hvd_residual_square_oracle_approximate`, and `SCOLHKG.Real.CumulativeRiskImplementation.providerRiskBlocks_vCPlus_conservative` |
| `OrthogonalHVD.singleton_evidence_mode="source_prior"` | A singleton target response cannot separate mean error from aleatoric variance. V14 therefore keeps its frozen replicated-source HVD prediction; only within-policy target replication replaces it with a nonnegative sample variance. | `source_prior_singleton_evidence_independent_of_mean_head` and `replicated_variance_evidence_nonnegative` in `SCOLHKG.Real.HVDImplementation` |
| `FiniteTaskModelEnsemble.variance_structure_posterior_mode="replication_only"` | V15 factorizes task uncertainty into a response-updated mean-structure posterior and a source/replication-only HVD-structure posterior. Prediction and certification mix aleatoric variance only with the latter; exact-KG samples their Cartesian-product law, and nested sample variances at one policy replace one sufficient-statistic record. | `variance_head_mixture_independent_of_mean_weights`, `replication_only_variance_score_independent_of_mean`, and `product_head_chance_bound` in `SCOLHKG.Real.HVDImplementation` |
| `variance.OrthogonalHVD.predict_certification_variance` | `base + model_uncertainty`, guarded by class variance and floor | `SCOLHKG.Real.HVDImplementation.certificationVariance_sound_from_model_uncertainty` |
| `variance.OrthogonalHVD.predict_decomposition`, factor mode | provider block diagnostics `floor/independent/shared/linear/tail_guard/v_C_plus` | `SCOLHKG.Real.CumulativeRiskImplementation.factorShockBlocks_total_eq_components`, `factorShockBlocks_shared_omission_underestimates`, `providerRiskBlocks_total_eq_components`, and `providerRiskBlocks_vCPlus_conservative` |
| `core.certification.conservative_chance_margin` | `mu_g + sqrt(beta_g)s_g + z sqrt(v_C^+) - tau` | `SCOLHKG.Real.CertificationImplementation.implementation_certifies_true_quantile` |
| `ObservableConstraintMeanBasis` plus expert `CumulativeRiskFeatureProvider` | Separate `eta` mean/epistemic and `psi` cumulative-risk coordinates joined only by the certified margin | `SCOLHKG.Real.MeanRiskCoordinateSeparation.separated_certificate_sound` and joint-coordinate noninterference lemmas |
| `ObservableStateExposure`, `SourceAlignedBoundaryCoordinate`, `SourceAlignedVarianceRiskCoordinate`, common template-free post-run pool, and `select_boundary_coordinate_candidates` | A target-observable state/trajectory record `e(x)` feeds independent `phi_mu=h_mu(e)` and `psi_v=h_v(e)` heads. The optional set descriptor is invariant to channel enumeration, while the diagonal-quadratic mean library represents curved boundaries. Source residual discrepancy can be carried by shared latent coefficient covariance plus a finite source-sample floor instead of an irreducible raw-policy deviation. The variance head is unchanged and emits cumulative `A,N`; provider exposure is an explicitly privileged upper bound. | `SCOLHKG.Real.BoundaryCoordinateSufficiency.shared_observable_exposure_preserves_both_heads`, `coordinate_heads_factor_through_shared_exposure`, `shared_observable_exposure_preserves_separated_margin`, `separated_coordinate_certificate_sound`, `shared_observable_exposure_separate_heads_sound`, both head-noninterference identities, fitted/oracle substitutions, candidate-support necessity/proposal restoration, `source_discrepancy_reference_variance_preserved`, and the nonnegativity/contraction lemmas for the latent discrepancy split |
| `source_constraint_mean_misspecification_mode` | Static V5 pilot residuals produce `kappa=max(1,(nu+q)/(nu+n0))`; source coefficient covariance and residual floor are scaled by `kappa`, and the directional variant adds a PSD rank-one covariance. V6 `hierarchical_predictive_scale` instead retains each frozen source law, accumulates charged target innovations, recomputes the full-history scale/evidence after every target call, and refits from that frozen law. It never recursively rescales an already-scaled posterior. The target null remains at unit scale, and exact-KG clones own independent sufficient statistics. | Static: `one_le_conservative_misspecification_scale`, `psd_directional_misspecification_can_only_increase`, `misspecification_floor_can_only_increase`, and `total_misspecification_uncertainty_can_only_increase`. Online: `online_misspecification_statistic_nonnegative`, `online_misspecification_statistic_accumulates`, `one_le_online_hierarchical_scale`, both `online_refit_*` theorems, and `target_null_unit_scale_is_unchanged` in `SCOLHKG.Real.BoundaryCoordinateSufficiency` |
| `source_constraint_mean_role_epistemic_mode="source_standardized_loss"` | Source-standardized unlabeled target role-matching loss defines trust `exp(-max(loss_per_coordinate-1,0)/2)`; effective source prior mass is requested mass times trust, and the remainder is transferred to `target:null`. It never reads target outcomes or oracle feasibility. | `role_support_trust_positive`, `role_support_trust_le_one`, and `role_support_cannot_increase_source_mass` in `SCOLHKG.Real.BoundaryCoordinateSufficiency` |
| `source_constraint_mean_misspecification_mode="source_contrast"` | Weighted disagreement among frozen source coefficient means is PSD-projected and added as one source-only low-rank random-effect covariance to every source component; target observations condition the resulting posterior but do not define this covariance. | `source_contrast_predictive_gain_nonnegative` and `source_contrast_can_only_increase_epistemic_variance` in `SCOLHKG.Real.BoundaryCoordinateSufficiency` |
| `constraint_mean_coordinate_kind="role_adaptive_ordered"` / `"role_adaptive_set_invariant"` | V8 fits the role coordinate and fallback independently on the same frozen source archive. An outcome-free target channel-cardinality audit selects the role model only when that cardinality occurred in a source domain; otherwise it selects the ordered or set-invariant fallback. The selector does not manufacture target support and does not mix coefficients across incompatible feature maps. | `supportAdaptiveCoordinate`, both selector reduction theorems, `support_adaptive_coordinate_preserves_property`, and `support_adaptive_certificate_sound` in `SCOLHKG.Real.BoundaryCoordinateSufficiency` |
| `observable_mean_latent_transform="source_tanh"` and `source_constraint_mean_null_geometry="target_pool"` | V12 selects a tanh temperature by source-domain holdout only, bounding every aligned latent feature. The deterministic unlabeled target role-matching pool defines an inverse-Gram geometry for `target:null`; scalar normalization preserves the isotropic law's average prior predictive variance instead of shrinking it. | `source_tanh_coordinate_abs_lt_one`, `target_geometry_preserves_average_predictive_variance`, `target_geometry_scale_nonnegative`, and `scaled_target_geometry_quadratic_nonnegative` in `SCOLHKG.Real.BoundaryCoordinateSufficiency` |
| `observable_mean_latent_transform="source_support_clip"` / `"source_support_residual"` | V13 selects a support quantile by source-domain holdout. The mean coordinate remains linear and unchanged inside the resulting source support; target extrapolation is clipped to that support. The residual variant appends one bounded support-overflow feature whose coefficient and uncertainty are conditioned only on charged target observations. | `source_support_clip_lower_bound`, `source_support_clip_upper_bound`, `source_support_clip_eq_self`, `source_support_overflow_nonnegative`, `source_support_residual_nonnegative`, `source_support_residual_lt_one`, and `source_support_residual_eq_zero_on_support` in `SCOLHKG.Real.BoundaryCoordinateSufficiency` |
| `observable_mean_descriptor_mode="role_transport"` and `source_constraint_mean_role_epistemic_mode="matching_uncertainty"` | V16 learns a variable-cardinality channel-to-role partial transport from source-domain channel-dropout replay. Target matching uses only an unlabeled exposure pool. Nonnegative matching loss, assignment entropy, and cardinality gap define a covariance multiplier at least one; the implementation applies it only to source mean epistemic covariance and leaves the HVD product posterior unchanged. | `partial_role_mass_nonnegative`, `role_mismatch_epistemic_scale_ge_one`, `role_mismatch_inflation_cannot_decrease_variance`, and `role_mismatch_inflation_preserves_psd_scalar` in `SCOLHKG.Real.BoundaryCoordinateSufficiency` |
| `observable_mean_descriptor_mode="role_intervention_transport"` and `source_constraint_mean_misspecification_mode="hierarchical_predictive_scale"` | V17 learns channel roles from source-only responses to one normalized low-frequency intervention library. Rectangular target channels use an entropic barycentric map, so one channel may span several source roles. Charged target residuals update only the independent mean-head misspecification posterior; source covariance scaling is lower-bounded by one, `target:null` is not scaled, and the HVD product posterior is unchanged. | `barycentricRoleResponse`, `barycentric_role_response_in_convex_hull`, `hierarchicalMeanVariance`, `target_null_mean_variance_is_not_inflated`, and `source_hierarchical_mean_variance_cannot_decrease` in `SCOLHKG.Real.BoundaryCoordinateSufficiency` |
| `observable_mean_target_residual_rank > 0` | V18 residualizes a canonical ordered target exposure dictionary against the frozen source mean design on a deterministic unlabeled target pool, then retains a low-rank SVD basis. Its coefficient prior is zero-mean and block-independent; charged target observations condition it normally, while HVD remains unchanged. | `zero_residual_prior_preserves_source_mean`, `independent_residual_cannot_reduce_predictive_variance`, `independent_residual_preserves_nonnegative_variance`, and `orthogonal_residual_energy_decomposition` in `SCOLHKG.Real.BoundaryCoordinateSufficiency` |
| `source_constraint_mean_residual_rank_posterior=True` | V19 expands every source and target-null coefficient law over nested ranks `K=0..R` on one maximum-rank basis. Charged target likelihoods update the joint expert/rank posterior; moment matching retains within-rank covariance and between-rank disagreement. | `nested_residual_coefficient_variance_nonnegative`, `rank_zero_uses_inactive_residual_variance`, `residual_rank_mixture_disagreement_cannot_reduce_variance`, and `target_evidence_rank_weights_are_probabilities` in `SCOLHKG.Real.BoundaryCoordinateSufficiency` |
| `observable_mean_role_assignment_posterior=True` | V20 enumerates every admissible target-channel injection into the frozen source role atlas. Source experts and `target:null` share the same assignment orbit; each atom activates only one low-dimensional role block, charged target observations update assignment mass, and target truth appears only in the post-run expressivity audit. The HVD product posterior remains independent. | `uniform_assignment_weights_sum_one`, `assignment_mixture_mean_permutation_invariant`, `assignment_mixture_disagreement_cannot_reduce_variance`, and `target_evidence_assignment_weights_are_probabilities` in `SCOLHKG.Real.BoundaryCoordinateSufficiency` |
| `source_constraint_mean_structure_score_mode="loo_predictive"` | V21 scores every frozen assignment atom by exact leave-one-out Gaussian prediction of charged target margins, normalizes exponentiated scores with the frozen structure prior, and then conditions every component on the complete charged history for downstream prediction. Every online update recomputes the scores from the original frozen laws. Target oracle values are excluded and the HVD product posterior remains independent. | `crossFittedScoreLikelihood`, `cross_fitted_score_likelihood_positive`, `cross_fitted_evidence_mass_positive`, `cross_fitted_assignment_weights_are_probabilities`, and `cross_fitted_score_likelihood_relabel` in `SCOLHKG.Real.BoundaryCoordinateSufficiency` |
| `observable_mean_role_assignment_prior="source_geometry"` | V22 enumerates assignment costs under the frozen source role prototypes on a deterministic unlabeled target exposure pool. The temperature is the median positive source-domain best/second-best normalized matching gap times a preregistered scale. Source experts and `target:null` use the same prior; charged target evidence may update it, while target truth remains post-run-only. | `geometryAssignmentLikelihood`, `geometry_assignment_likelihood_positive`, `lower_geometry_cost_has_no_less_likelihood`, and `geometry_assignment_prior_is_probability` in `SCOLHKG.Real.BoundaryCoordinateSufficiency` |
| `source_constraint_mean_structure_score_mode="geometry_conditional"` | V23 freezes the source-geometry assignment marginal and updates only the conditional source/null expert posterior within each assignment. Target labels therefore calibrate source-mean misspecification without redefining channel roles; assignment and expert disagreement both enter the moment-matched epistemic covariance, while HVD remains independent. | `factorizedAssignmentExpertWeight`, `factorized_assignment_expert_weight_nonnegative`, `factorized_assignment_marginal_is_fixed`, `factorized_assignment_expert_weight_sum_one`, and `conditional_expert_evidence_cannot_change_assignment_marginal` in `SCOLHKG.Real.BoundaryCoordinateSufficiency` |
| `geometry_conditional` plus `hierarchical_predictive_scale` | V24 recomputes each source expert's noncontractive misspecification scale from its frozen law and the complete charged target history, refits source/null conditionals, and preserves the geometry assignment marginal exactly after every online call. | `hierarchical_misspecification_refit_preserves_assignment_marginal`, together with the V6 scale/refit and V23 factorization theorems in `SCOLHKG.Real.BoundaryCoordinateSufficiency` |
| `observable_mean_role_assignment_prior="source_geometry_boundary"` plus assignment-conditional `source_contrast` | V25 combines the source-geometry prior with a Gaussian likelihood for Fisher-transformed channel/chance-margin associations computed from the ordinary charged pilot. The updated assignment law is then frozen while source/null experts adapt online. Source-contrast covariance is fitted separately inside each assignment orbit, so mutually exclusive role blocks cannot acquire cross-assignment discrepancy covariance. | `boundaryRoleLogLikelihood`, `boundaryRoleLikelihood`, both channel-relabel theorems, `boundary_role_assignment_weights_are_probabilities`, and `assignment_conditional_contrast_zero_outside_active_block` in `SCOLHKG.Real.BoundaryCoordinateSufficiency` |
| `observable_mean_descriptor_mode="exchangeable_equivariant"` | V26 discards source-domain channel labels after fitting: each source expert is reduced to one exchangeable coefficient-block mean/covariance, copied to every target channel. The scalar mean remains linear. Only charged target responses may differentiate target channel blocks; hierarchical predictive scaling is refit from each frozen source law, can only inflate source uncertainty, and leaves `target:null` and the replication-only HVD product head structurally separate. | `exchangeableChannelLinearMean`, `exchangeable_channel_linear_mean_relabel`, `exchangeableChannelLogScore`, `exchangeable_channel_log_score_relabel`, and `exchangeable_channel_prior_score_relabel` in `SCOLHKG.Real.BoundaryCoordinateSufficiency`; executable diagnostics audit source-block equality, target-block differentiation, target-oracle exclusion, misspecification monotonicity, and variance-head isolation. Held-out sufficiency is an empirical gate. |
| `source_constraint_mean_adaptation_mode="sequential_aggregate_hyperlaw"` | V27 marginalizes source-domain identity once into a single exchangeable Gaussian hyperlaw. Its covariance contains within-source uncertainty plus between-source coefficient displacement. Every paid target observation recomputes conservative misspecification from the same frozen law and the complete target history, then conditions the one-atom posterior. There is no source selector or `target:null` atom. | `empiricalBayesProjectedMean`, `empiricalBayesProjectedVariance`, `empirical_bayes_projected_variance_nonnegative`, both `singleton_empirical_bayes_*` identities, and `append_one_target_observation_count` in `SCOLHKG.Real.BoundaryCoordinateSufficiency`; executable diagnostics additionally require one `source:aggregate` atom, zero between-component covariance, identity projection, target counts `10..20`, and exactly ten online refits. |
| `source_constraint_mean_hyperlaw_mode="shared_low_rank_discrepancy"` | V42 uses `sum_s w_s^2 C_s` for uncertainty in the shared coefficient estimate, retains exchangeable channel-role covariance separately, and uses only between-domain coefficient displacement for a discrepancy factor of rank at most `S-1`. It therefore does not reinterpret each source fit covariance as a fresh target-task draw. | `weightedSharedEstimationVariance`, `shared_estimation_variance_le_legacy_transfer`, `betweenDomainProjectedVariance`, `sourceDomainDiscrepancyProjectedVariance`, `sharedLowRankHyperlawProjectedVariance`, and their nonnegativity/separation theorems in `SCOLHKG.Real.SharedLowRankSourceHyperlaw`; Python tests require PSD decomposition, channel-permutation equivariance, rank bound, oracle exclusion, and explicit runner selection. |
| `source_constraint_mean_hyperlaw_mode="shared_low_rank_predictive"` | V43 converts the weighted between-source population covariance into the predictive discrepancy of a newly drawn held-out task using `c=sum_s w_s^2` and multiplier `(1+c)/(1-c)`. It is unavailable for fewer than two effective sources or `c=1`, retains the same `S-1` factor rank, and never uses target labels or truth. | `finiteSourcePredictiveMultiplier`, `finite_source_predictive_multiplier_nonnegative`, `finite_source_predictive_multiplier_at_least_one`, `finiteSourcePredictiveProjectedVariance`, its nonnegativity/dominance theorems, and `finiteSourcePredictiveLowRankVariance` in `SCOLHKG.Real.SharedLowRankSourceHyperlaw`; Python tests require the exact multiplier, PSD covariance, permutation equivariance, rank preservation, one-source rejection, oracle exclusion, and explicit runner selection. |
| `meta_source_budget_mode="per_base_domain"` with source task episodes | V45 partitions each base source domain's frozen record budget across multiple source-only task-context draws. The context law perturbs normalized boundary centers and radius before the held-out target is observed; target initialization and all charged target actions remain paired. Episode labels without geometry perturbation are a required negative control. | `sourceSimulatorCallBudget`, `sourceEpisodeTaskCount`, `sourceEpisodeDiscrepancyCapacity`, `equal_episode_allocation_preserves_source_call_budget`, `source_episode_capacity_monotone`, and `episode_discrepancy_loading_uses_capacity` in `SCOLHKG.Real.SharedLowRankSourceHyperlaw`; Python audits exact 384-call equality, deterministic per-episode allocation, frozen source fingerprints, target-oracle exclusion, target action/response pairing, and strict improvement over both the two-task baseline and label-only control. |
| `source_constraint_mean_hyperlaw_mode="grouped_task_discrepancy"` or `"grouped_task_predictive"` | V46 treats frozen episodes as repeated tasks nested within their base source domains. Source-fit covariance contracts only the shared base mean; base-mean displacement, centered within-base episode contrasts, and exchangeable channel-role covariance remain distinct target-variation blocks. The predictive ablation corrects between- and within-level finite-task scales separately. | `withinBaseTaskProjectedVariance`, `within_base_task_projected_variance_nonnegative`, `within_base_task_contrast_invariant_to_base_offset`, `groupedSourceTaskProjectedVariance`, its separation theorem, and `groupedTaskDiscrepancyCapacity` in `SCOLHKG.Real.SharedLowRankSourceHyperlaw`; Python tests require PSD blocks, base-offset invariance, rank bounds, explicit mode selection, fixed source cost, target-oracle exclusion, and paired target traces. |
| `source_constraint_mean_hyperlaw_mode="grouped_task_deconvolved"` or `"grouped_task_deconvolved_predictive"` | V47 subtracts analytically propagated short-episode coefficient-fit covariance from the channel-role, between-base, and within-base random-effect blocks before separate PSD projection. The same fit covariance remains a positive contribution to uncertainty in the shared source mean, preventing both omission and double counting. | `randomEffectsDeconvolvedProjectedVariance`, `random_effects_deconvolved_projected_variance_nonnegative`, `random_effects_deconvolution_no_larger_than_observed`, `random_effects_deconvolution_recovers_latent_variance`, and `random_effects_deconvolution_removes_noise_dominated_direction` in `SCOLHKG.Real.SharedLowRankSourceHyperlaw`; Python audits nonzero observed/noise traces, corrected trace contraction, PSD output, explicit mode selection, fixed source cost, target-oracle exclusion, and paired target traces. |
| `certification_head_authority` | V28 assigns constraint mean and epistemic variance exclusively to the single aggregate target GPR. `split_gpr_task_hvd` retains only the task-robust aleatoric HVD head; `split_gpr_cumulative_hvd` uses only direct provider-based cumulative HVD. Legacy task-joint mean/epistemic moments remain counterfactual diagnostics and cannot enter candidate filtering, recommendation, truth-pool certification, hard terminal values, or posterior Bayes-risk terminal values. | `HvdHeadAuthority`, `authoritativeHvdVariance`, `separatedConstraintUpper`, `separatedBayesMarginMean`, both legacy-head noninterference theorems, and both exact HVD-authority reduction theorems in `SCOLHKG.Real.BoundaryCoordinateSufficiency`; Python tests inject adversarial legacy task moments and require identical recommendation/terminal split-head results. |
| `SingleOLHKGAlgorithm._cantelli_dominance_lower_bound` and posterior-dominance incumbent updates | V29 upper-bounds the posterior variance of an incumbent/challenger Bayes-risk difference without assuming independence, and switches only when the one-sided improvement lower bound reaches `1-delta`. The terminal recommendation is the sequentially maintained incumbent; all diagnostics exclude target oracle information. | `covarianceFreeDifferenceVarianceUpper`, `covariance_free_difference_variance_upper`, `cantelli_improvement_and_false_switch_sum_one`, and `accepted_cantelli_switch_controls_posterior_error` in `SCOLHKG.Real.BoundaryCoordinateSufficiency`; the final implication is explicitly conditional on the Cantelli probability bound and therefore does not overstate posterior calibration. |
| `predictive_scale_upper*`, `predictive_sandwich_hc3*`, and `predictive_scale_sandwich_hc3*` source-mean calibration | V30 applies a finite-sample scale clipped below at one; V31 adds a local HC3 coefficient covariance after target conditioning; V34 first relaxes the source hyperlaw before conditioning and then adds the HC3 correction to the same posterior. Charged target data are reused through one full-history refit, target oracle values are excluded, and the cumulative HVD head remains separate. | `posteriorScaleVariance`, `posterior_scale_at_least_one_cannot_reduce_variance`, `hc3ProjectedCorrection`, `hc3_projected_correction_nonnegative`, `sandwichCorrectedVariance`, `sandwich_correction_cannot_reduce_variance`, `sandwichCorrectedMean`, `sandwich_correction_preserves_conditioned_mean`, `robustEmpiricalBayesProjectedVariance`, and `robust_empirical_bayes_two_layer_variance_nonshrinking` in `SCOLHKG.Real.BoundaryCoordinateSufficiency`. Python additionally audits HC3 leverage, finite-target/source multipliers, PSD projection, full charged-history refitting, and exact-KG clone state. |
| `posterior_dominance_initialization="certificate_lexicographic"` and `"certified_only"` | V32's legacy negative ablation chooses among canonical theory-certified initial points but records an explicitly uncertified fallback when that set is empty. V48 is theorem-aligned: `certified_only` represents the protected incumbent as an option, returns `none` for an empty certified set, and lets the ordinary posterior Bayes terminal rule act without claiming preservation. | `certifiedInitialIndices`, `mem_certified_initial_indices_iff`, `certified_initializer_is_safe_when_certificate_nonempty`, `empty_certificate_has_no_certified_initializer`, `CertifiedOnlyInitializerContract`, `certified_only_initializer_some_is_safe`, `certified_only_initializer_none_requires_empty`, and `certified_only_initializer_cannot_fabricate_incumbent` in `SCOLHKG.Real.BoundaryCoordinateSufficiency`. |
| `decision_aleatoric_mode="posterior_central"` and `decision_violation_loss_mode="failure_probability"` | V49 separates the cumulative-HVD posterior used by a Bayes action from the conservative `v_C_plus` used by theory certification. The alternative terminal loss is the posterior expectation of a binary chance-failure penalty, `objective + rho * P(G > 0)`. Static Sobol and exact evaluate-or-replicate arms independently identify the HVD-view and loss effects; neither mode may alter the certification margin. | `posteriorCentralAleatoricMargin`, `certificationUpperAleatoricMargin`, `certification_upper_margin_independent_of_decision_variance`, `central_decision_margin_cannot_relax_upper_certificate`, `posteriorBinaryChanceRisk`, `posterior_binary_chance_risk_monotone`, and `posterior_binary_chance_risk_excess_nonnegative` in `SCOLHKG.Real.BoundaryCoordinateSufficiency`. |
| `decision_ambiguity_mode="posterior_nominal"` | V50 removes the KL ambiguity premium from the Bayes action only. Certification and its robust chance margin remain unchanged. The implementation logs nominal and robust loss components for every terminal candidate, so the ambiguity premium and posterior rank movement are auditable without target truth. | `posteriorNominalDecisionRisk`, `klRobustDecisionRisk`, `kl_robust_decision_risk_decomposition`, `posterior_nominal_decision_risk_le_kl_robust`, and `certification_upper_margin_independent_of_decision_ambiguity` in `SCOLHKG.Real.BoundaryCoordinateSufficiency`. |
| `evaluate_or_replicate_new_action_policy="canonical_plus_posterior_risk"` | V51 expands the finite exact-VOI action set from one canonical Sobol point to a nested posterior-only shortlist while retaining the same eligible replication actions. Candidate generation and shortlist ranking exclude target truth; exact fantasy refits still choose the action. Every active arm and its signed VOI are logged. | `finite_action_set_expansion_cannot_reduce_best_voi` in `SCOLHKG.Real.BoundaryCoordinateSufficiency` proves that maximizing the same VOI over the enlarged finite action set cannot reduce the best available value. |
| `predictive_scale_sandwich_hc3*_confidence` | V35 recognizes the sandwich matrix as a robust confidence covariance rather than extra generative noise. Bayes expected-violation ranking uses the central scaled-hyperlaw covariance; theory certification and covariance-free Cantelli switch variance retain the dominating robust covariance. Both are deterministic views of the same charged-data refit, and target oracle values are absent. | `centralPredictiveVariance`, `robustConfidenceVariance`, `robust_confidence_variance_dominates_central_prediction`, `confidenceOnlyBayesRiskVariance`, and `confidence_only_correction_does_not_change_bayes_ranking_variance` in `SCOLHKG.Real.BoundaryCoordinateSufficiency`; Python tests verify central/robust covariance cloning, robust noncontraction, central Bayes-risk use, and robust certificate use. |
| `source_constraint_mean_coefficient_prior=True` | frozen source-domain coefficient means, within-domain covariance, between-domain disagreement, and a covariance floor define the low-rank `eta` prior; charged target labels condition the same `ParametricGPR` posterior, while `psi=(A,N)` remains the cumulative-risk coordinate | `SCOLHKG.Real.SourceConstraintMeanPosterior.hierarchicalSourceDirectionalVariance_nonnegative`, `target_conditioning_shrinks_source_directional_variance`, `sourceTargetPosteriorMean_eq_precision_weighted`, and `source_posterior_certificate_sound` |
| `source_constraint_mean_adaptation_mode="evidence_mixture"` | each frozen source-domain coefficient law and one domain-independent null component are conditioned on the same charged target pilot; exact linear-Gaussian marginal evidence updates component mass, and the posterior is projected by matching mean and covariance, including between-component disagreement | `SCOLHKG.Real.SourceConstraintMeanPosterior.finiteMixtureDirectionalVariance_nonnegative` and `mixture_disagreement_cannot_reduce_directional_variance` |
| `source_constraint_mean_adaptation_mode="sequential_evidence_mixture"` | retain the finite source/null Gaussian components after the pilot; every charged target response multiplies current component mass by its predictive likelihood, normalizes the law, conditions every component, moment-matches only for downstream GPR APIs, and shares the current law with cumulative HVD and exact posterior clones | `SCOLHKG.Real.SourceConstraintMeanPosterior.sequentialMixtureWeight_nonnegative`, `sequentialMixtureWeight_sum_one`, `finiteMixtureDirectionalVariance_nonnegative`, and `mixture_disagreement_cannot_reduce_directional_variance` |
| post-run certification-vacuity audit | with the true chance mean substituted only after optimization, a point can be certified only if its epistemic radius does not exceed its true safety depth; equivalently `beta_g * v_epi <= safety_depth^2` is necessary | `SCOLHKG.Real.CertificationImplementation.oracle_mean_certifiable_requires_epistemic_radius_le_safety_depth`, `oracle_mean_certificate_vacuous_of_shallow_safety`, and `oracle_mean_certifiable_requires_epistemic_variance_threshold` |
| `hvd_source_task_weight_mode="constraint_mean"` | the target-pilot posterior over source domains is shared by the constraint-mean coefficient mixture and cumulative HVD shape dictionary; no-transfer mass maps to a nonnegative target-pooled null shape instead of being redistributed over source shapes | `SCOLHKG.Real.SourceShapeMixtureHVD.shared_task_posterior_preserves_nonnegative_shape` and `sourceShapeWithTargetNull_nonnegative` |
| `acquisition.OLHKGAcquisition.score` | additive proxy retained for ablation | `SCOLHKG.Real.AdditiveApproxKG.additive_proxy_maximizer_exact_gap_le_two_eta` |
| `algorithms.SingleOLHKGAlgorithm._solve_posterior_recommendation` | choose lowest posterior objective among robust chance-feasible candidates | `SCOLHKG.Real.PosteriorRecommendation.robust_feasible_implies_posterior_certified` and `robust_argmin_is_objective_minimizer_on_robust_set` |
| `core.candidates.posterior_sample_candidates` | finite posterior candidate pool from sampled parametric coefficients | `SCOLHKG.Measure.PosteriorCoefficientSampler.posteriorCoefficientSampler_bad_event_le_sum` and `SCOLHKG.Measure.PosteriorSamplingCandidates.randomAdaptiveCenteredSubGaussian_bad_event_le_sum` control random candidate sets by deterministic envelope pools |
| posterior coefficient draw law | sampled parametric coefficient vector with mean/covariance from GPR posterior | `SCOLHKG.Measure.PosteriorMultivariateGaussian` uses mathlib `multivariateGaussian` to prove the draw law, mean, covariance, and Gaussian linear scores |
| finite candidate/kernel budget | scalar information gain `0.5 log(1+var/noise)` accumulated over finite steps | `SCOLHKG.Real.FiniteKernelInformationGain.finiteInformationGain_le_uniform_cap`, `finiteInformationGain_eq_determinantInformationGain_product`, `SCOLHKG.Real.KernelDeterminantBridge.finiteInformationGain_le_determinant_cap`, and `SCOLHKG.Real.FeatureKernelDeterminantCap.finiteInformationGain_le_feature_map_norm_cap` |
| `SingleOLHKGAlgorithm._exact_posterior_update_scores` | MC estimate of current terminal certified value minus updated terminal certified value after GPR/HVD update | `SCOLHKG.Measure.PosteriorUpdateKG.posterior_update_kg_maximizer_is_exact_kg_maximizer` defines the exact target; `SCOLHKG.Real.ExactKGImplementation.exact_mc_estimator_maximizer_gap` bridges uniformly accurate MC estimates; `SCOLHKG.Measure.ExactMCConcentration.exactMC_constant_radius_bad_event_le_sum` gives finite-pool concentration |
| shared `terminal_pool` plus posterior frontier experiment actions in `SingleOLHKGAlgorithm.run` | the current recommendation, every hypothetical update, and the realized post-update recommendation use the same history-measurable terminal pool; posterior-only frontier actions are included in the experiment set | `SCOLHKG.Measure.SharedTerminalPoolKG.shared_terminal_pool_gain_uses_pre_state_pool`, `shared_terminal_pool_maximizer_is_one_step_optimal`, `terminal_frontier_subset_closed_experiments`, and `original_experiments_subset_closed_experiments` |
| `FiniteTaskPosterior.update_from_predictive` | positive generalized-Bayes expert mass followed by simplex normalization | `SCOLHKG.Real.TaskPosterior.generalizedBayes_normalized_support` and `normalizeFiniteWeights_sum_eq_one` |
| `FiniteTaskLatentPosterior` shadow/authoritative joint update | positive normalized generalized-Bayes mass on `(structural expert, signed-bias-function/scale/loss class)`, normalized nonnegative marginals, source-frozen scalar or `b(psi)` bias used only by Bayes decisions, and authoritative epistemic scaling by `max(1,c_scale)^2` that cannot relax the theory margin | `SCOLHKG.Real.JointTaskLatentPosterior` |
| V4 expert-conditional calibration posterior | charged standardized residuals update one conjugate precision/information pair per structural expert; posterior coefficient means affect Bayes ranking only, while `predictive_sd^2 phi^T P^{-1} phi >= 0` is added to theory epistemic variance and exact-KG clones the same state | `SCOLHKG.Real.JointTaskLatentPosterior.expert_calibration_precision_update_ge_prior`, `adaptive_theory_margin_ignores_calibration_mean`, and `adaptive_calibration_covariance_cannot_relax_theory_margin` |
| V28 `Q_pred` / `Q_safe` update and decision split | two independently normalized full-support generalized-Bayes masses; clipped threshold/pairwise losses lie in `[0,-log(epsilon)]`; objective aggregation uses `Q_pred`, while proposals/certification/exact KG use `Q_safe` | `SCOLHKG.Real.SafeGeneralizedTaskPosterior.predictiveTaskMass_sum_eq_one`, `safeDecisionTaskMass_sum_eq_one`, `clipped_probability_log_loss_bounded`, `safe_generalized_pac_bayes_bound_on_moment_event`, and `safeDecisionJointBelief_uses_safe_weight` |
| `FiniteTaskPosterior.proposal_weights` plus expert-mixture initial/sequential/terminal proposals | `(1-epsilon) Q_t + epsilon Pi` is normalized and retains at least `epsilon * Pi(xi)` mass on every prior-supported expert | `SCOLHKG.Real.TaskPosterior.finite_task_proposal_normalized`, `finite_task_proposal_preserves_prior_support`, and `finite_task_proposal_positive_of_prior_positive` |
| `FiniteTaskPosterior.mixture_moments` | `E_Q[s_k^2] + Var_Q[m_k] + E_Q[v_k]` | `SCOLHKG.Real.TaskPosterior.task_total_variance_is_within_between_aleatoric` |
| `FiniteTaskPosterior.kl_robust_expectation` and robust certification | entropic dual upper bound over `KL(q||p) <= rho`, followed by robust moment certification | `SCOLHKG.Real.TaskPosterior.finiteTaskKL_nonnegative`, `kl_ball_entropic_upper`, `finite_pac_bayes_bound_on_moment_event`, `SCOLHKG.Measure.TaskPACBayes`, and `robust_certificate_holds_for_every_admissible_task_posterior` |
| finite task-posterior exact-MC branch | sample expert identity, clone/update task weights plus every expert GPR/HVD, then recompute robust terminal value | `SCOLHKG.Real.TaskPosterior.joint_task_exact_mc_zero_error_is_one_step_optimal` plus the existing MC concentration layer |
| `exact_kg_sampling_mode=stratified_expert` | enumerate every finite expert with its posterior decision weight and use common antithetic Gaussian innovations within expert | `SCOLHKG.Real.StratifiedExpertKG.finite_stratified_identity_has_no_categorical_error` and `finite_stratified_error_le_conditional_error` prove that the categorical layer is exact and only conditional Gaussian error remains |
| `LearnedMetaPrior.ordered_cumulative_risk_exposure` and the `ordered_cumulative` finite expert | source-selected global plus low-frequency positional exposure is used by the same expert GPR, factor-HVD, certificate, proposal family, and exact KG update | `SCOLHKG.Real.OrderedCumulativeExposure.zero_frequency_is_aggregate_exposure`, `selected_frequency_is_positional_exposure`, and `ordered_coordinate_uses_cumulative_risk_decomposition` |
| `FixedTaskExpertBasis` ordered semiparametric coefficient-nullspace projection | bounded local RBF center coefficients are projected into the nullspace of the finite ordered/kernel cross matrix before entering the same capped expert | `SCOLHKG.Real.OrthogonalSemiparametric.coefficientNullspace_orthogonal_all` proves finite-design orthogonality, while `finiteKernelCombination_abs_le_card` gives a candidate-independent finite amplitude bound |
| `AdaptiveSpikeSlabPosterior.fit` total-rank projection | reserves the fixed prefix inside the `max_effective_fraction * N` budget, then projects optional inclusion probabilities onto the remaining budget | `SCOLHKG.Real.AdaptiveCoefficientSparsity.fixed_prefix_and_optional_budget_control_total_dimension` and `fraction_budget_controls_total_dimension` |
| adaptive PIP cardinality bracketing and damping | dynamically reaches the minimum-PIP endpoint before bisection, then damps the previous and proposed budget-feasible vectors | `SCOLHKG.Real.AdaptiveCoefficientSparsity.constant_inclusion_floor_is_budget_feasible` and `damping_preserves_effective_dimension_budget` prove endpoint feasibility and preservation under damping |
| `AdaptiveGroupRidgePosterior` nested target refits | full LOO refits choose one isotropic ridge penalty per `A`, `A^2`, and `N` group from a fixed finite grid; analytic target truth is never read | `SCOLHKG.Real.GroupRidgeComplexity.finiteRidgeEffectiveDimension_le_feature_count` bounds learned effective df and `finite_nested_selector_oracle_bound` gives the finite-selector `2 epsilon` oracle inequality |
| V29 budgeted finalist replication | freeze posterior-only finalists before new labels, spend only the final reserved stages to balance replicate counts, and use `ybar_g + z_alpha sigma + z_delta sigma/sqrt(r) - tau` only when the theory-certified set is empty | `SCOLHKG.Real.FinalistReplication.replicated_finalist_margin_sound_on_joint_event`, `frozen_finalists_ignore_future_labels`, `one_replication_strictly_reduces_positive_deficit`, and `reserved_finalist_stage_stays_inside_total_budget` |
| V30 expert-stratified safety nomination | every finite structural expert nominates its own minimum predicted-violation action before posterior-mass-free finalist ranking | `SCOLHKG.Real.FinalistReplication.every_finite_expert_nomination_is_supported` and `nomination_support_does_not_depend_on_posterior_mass` |
| V31 history-measurable adaptive finalist race | refresh the expert challenger before each paid suffix observation, archive every tested action, and compare only candidates that meet the replication contract | `SCOLHKG.Real.FinalistReplication.adaptive_archive_contains_every_nomination`, `adaptive_archive_card_le_initial_add_refreshes`, `incomplete_finalist_cannot_enter_completed_race`, `completed_adaptive_finalist_sound_on_joint_event`, and `adaptive_finalist_bad_event_le_sum` |
| V32 fixed-universe adaptive race | freeze the finite terminal action universe before suffix labels and rerank only that universe after each paid update | `SCOLHKG.Real.FinalistReplication.adaptive_archive_subset_fixed_universe` and `adaptive_archive_card_le_fixed_universe` |
| Oracle-free two-stage source-consensus baseline | use `n0` source-informed initial-design calls and `N-R-n0` adaptive state-coupled exact-KG calls, freeze the terminal universe, spend `R` charged calls on heteroscedastic ranking-and-selection, and report posterior/replication certification separately from least-risk fallback | `SCOLHKG.Real.TwoStageDecision` proves the budget partition, terminal semantics, certified safety, fallback relative-risk bound, and deterministic regret decomposition; `SCOLHKG.Measure.TwoStageDecision` proves finite verification concentration and high-probability event transfer |
| TCB-V2 hierarchical boundary plus V33 three-layer repair | `HierarchicalSignedDistancePosterior` learns one source boundary shape with target location, positive scale, optional planar rotation, and orthogonal low-rank residual; `decision_contract_mode=certified_lexicographic` makes main exact KG, coverage-reserved finalist nomination, suffix KG, and final recommendation consume the same authoritative upper margin, while every fantasy refits the same adapter | `SCOLHKG.Real.HierarchicalBoundaryCertificate` proves positive scale, planar-rotation norm preservation, nonnegative predictive covariance, upper-margin monotonicity under rotation/residual uncertainty, reserved-frontier order, frontier/terminal/recommendation coherence, recommendation safety under upper coverage, and lexicographic certified-action dominance |
| TCB-V3 finite boundary-family posterior | `BoundaryFamilyMixturePosterior` supports broad and atomic source-frozen libraries, updates only family mass from leave-one-pilot-out target evidence, ranks by posterior mean, and certifies with a `1-delta_family` credible-family upper envelope plus nonnegative guard | `SCOLHKG.Real.BoundaryFamilyMixtureCertificate` proves credible-mass accounting, envelope coverage when the true family remains credible, guard monotonicity, safe recommendation, target-name noninterference, and failure probability at most `delta_family + alpha` |
| TCB-V4 continuous boundary-family synthesis | `BoundaryFamilySynthesisPosterior` freezes one canonical signed-distance atom per source domain, learns a source coefficient prior, updates a held-out intercept and nonnegative atom coefficients from ordinary pilots, and adds coefficient covariance plus residual uncertainty to its Student-t upper margin | `SCOLHKG.Real.BoundaryFamilySynthesisCertificate` proves nonnegative-synthesis monotonicity, nonnegative predictive variance, upper-margin non-relaxation, recommendation safety under coverage, and target-name noninterference |
| TCB-V5 orthogonal semiparametric boundary | `BoundaryFamilySemiparametricPosterior` adds one or two frozen RBF residual coordinates in source-family score space, projects their center coefficients into the source-design cross nullspace, updates only low-dimensional target coefficients, and includes both covariance blocks plus remaining noise in the upper margin | `SCOLHKG.Real.OrthogonalSemiparametric` proves coefficient-nullspace orthogonality and bounded kernel combinations; `SCOLHKG.Real.BoundaryFamilySemiparametricCertificate` proves nonnegative direct-sum predictive variance and recommendation safety under coverage |
| Noise-limited certifiability audit | `core.oracle_certification.oracle_certifiability_metrics` evaluates `m + q sigma / sqrt(R)` and the closed-form known-variance replication burden; `benchmark_certifiability_coordinate_audit.py` keeps strict, target-oracle, and provider-upper-bound strata separate | `SCOLHKG.Real.OracleCertifiability` proves radius nonnegativity/replication monotonicity, squared-budget sufficiency, and persistence of certification at larger budgets |
| Frozen source-consensus proposal and committed safety shortlist | `LearnedMetaPrior._fit_source_consensus_templates` aggregates within-source empirical chance-margin and objective ranks over one shared archive; strict controls use `source_design_mode=shared_uniform`, so byte-identical unrestricted policies are observed in every source domain; `risk_objective_initial_candidates` selects source safety-objective Pareto elites before dimension-free maximin filling; `initial_universal_candidates` retains the legacy rank-spanning design; `observed_safety_reserved` restricts its protected shortlist to charged members of that design | `SCOLHKG.Real.SourceConsensusCommit.strictlyMonotone_order_invariant` proves domainwise rank invariance under every strictly increasing rescaling; the safety-objective scalarization lemmas prove weak/strict Pareto monotonicity; `shared_source_design_domain_noninterference` and its membership lemma formalize the paired archive; the remaining noninterference, ordering, and commitment theorems prove the frozen shortlist contract |
| `observable_mean_mode="source_rank"` | interpolate within-source empirical chance-margin percentile ranks in the fixed observable-policy library, then expose consensus safety rank and cross-source disagreement to the charged-target posterior | `SCOLHKG.Real.SourceConsensusCommit.strictlyMonotone_order_invariant`, `weightedSourceRank_mem_unit_interval`, and `sourceConsensusScore_le_three_halves` prove scale invariance and the finite coordinate range used by the implementation |
| `AdaptiveSpikeSlabPosterior.fit` semantic group-shared shrinkage | one joint Bayes factor, PIP, and isotropic slab/spike scale are shared within each declared `A^2` or `N` block, while target data learn the coefficient direction | `SCOLHKG.Real.GroupSharedShrinkage.sharedGroupPenalty_rotation_invariant` and `sharedGroupEffectiveDimension_eq` prove within-group coordinate invariance and full coefficient-budget accounting |
| `TrafficTrajectoryEncoder` fresh CSV aggregate | state-action occupancy plus queue/wait/flow and demand-shock exposure | `SCOLHKG.Real.TrafficTrajectoryModel.totalRisk_decomposition`, `sharedShock_omission_underestimates`, and `TrafficLogSchemaRow` formalize the finite traffic risk model and CSV schema semantics |

The exact posterior-update SC-OLH-KG object is formalized in
`SCOLHKG.Measure.PosteriorUpdateKG`, and the Python runner now defaults to the
MC estimator through `acquisition_mode=exact_mc` and `exact_kg_mc_samples=8`.
The additive proxy above is now explicitly an ablation; if exact-MC is too
expensive in a table, the manuscript must cite the `2 eta` approximation lemma
instead of presenting additive as the main mathematical object.

## Code-To-Theory Closure Notes

1. `compute_h` now emits and validates a checkable certificate, the validator
   conditions are Lean-bridged, pop/push cut-order preservation is Lean-proved,
   concrete intersection/pop/split branch certificates are Lean-proved, and
   final global dominance implies exact KG without runtime validation.  The
   full recursive sorted-line fold is also Lean-proved: popped active lines are
   pointwise dominated by the final output stack, and output endpoint dominance
   lifts to `FinalEnvelopeStackInvariant` over all original input lines.
2. The posterior-sampling candidate generator now has both pieces: mathlib
   multivariate-Gaussian coefficient law and finite selector/envelope
   containment.  The remaining manuscript choice is only the exact final
   posterior covariance parameterization to describe in prose.
3. Bounded and generic sub-exponential residual-square interfaces are
   available, and the default radius is exposed in code/proof with a
   closed-form inversion theorem.
4. The exact KG estimator is the Stage-I search acquisition and has both a
   finite-pool concentration theorem and an MC-schedule variance theorem. The
   reserved suffix is separately modeled as finite heteroscedastic
   ranking-and-selection; the new `two_stage_decision` result block audits this
   implementation contract without pretending the full run is exact KG.
5. The traffic encoder/log parser, SUMO trajectory logger, schema-row
   contract, and finite traffic-risk Lean model are implemented.  The remaining
   work is to generate the server-side fresh-seed CSV artifact and include its
   encoded table in the paper package.
