\section{Code and Data Disclosure}\label{sec:code-data}

Code and the declared seeds generate every synthetic instance.  The Energy
study uses the public data cited in Section~\ref{sec:experiments}.  An
anonymous package provides code, fixed manifests, compact results, and build
instructions; raw checkpoints are unnecessary.  A persistent identifier will
replace the anonymous location upon acceptance.
